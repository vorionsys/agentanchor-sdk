---
"@vorionsys/agentanchor-sdk": patch
---

Extract `agentanchor-sdk` into its own standalone repository (`vorionsys/agentanchor-sdk`) with tokenless OIDC trusted publishing + provenance. Bump `@vorionsys/shared-constants` to `^2.0.0`. Relicensed Apache-2.0.
